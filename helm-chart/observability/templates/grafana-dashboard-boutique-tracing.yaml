{{- if and .Values.grafanaDashboards.enabled .Values.grafanaDashboards.boutiqueTracing.enabled }}
{{- $environment := required "environment must be set through values-dev.yaml or values-prod.yaml" .Values.environment }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: boutique-distributed-tracing-{{ $environment }}
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
    app.kubernetes.io/part-of: observability
    app.kubernetes.io/component: grafana-dashboard
    environment: {{ $environment | quote }}
  annotations:
    grafana_folder: {{ .Values.grafanaDashboards.boutiqueTracing.folder | quote }}
data:
  boutique-distributed-tracing-{{ $environment }}.json: |
{{ .Files.Get "dashboards/boutique-distributed-tracing.json"
   | replace "__ENVIRONMENT__" $environment
   | replace "__NAMESPACE__" .Values.grafanaDashboards.boutiqueTracing.namespace
   | indent 4 }}
{{- end }}
