{{- if and .Values.grafanaDashboards.enabled .Values.grafanaDashboards.argoRolloutsOperations.enabled }}
{{- $environment := required "environment must be set through values-dev.yaml or values-prod.yaml" .Values.environment }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: argo-rollouts-operations-{{ $environment }}
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
    app.kubernetes.io/part-of: observability
    app.kubernetes.io/component: grafana-dashboard
    environment: {{ $environment | quote }}
  annotations:
    grafana_folder: {{ .Values.grafanaDashboards.argoRolloutsOperations.folder | quote }}
data:
  argo-rollouts-operations-{{ $environment }}.json: |
{{ .Files.Get "dashboards/argo-rollouts-operations.json"
   | replace "__ENVIRONMENT__" $environment
   | replace "__NAMESPACE__" .Values.grafanaDashboards.argoRolloutsOperations.namespace
   | indent 4 }}
{{- end }}
