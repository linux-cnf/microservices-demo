{{- if and .Values.grafanaDashboards.enabled .Values.grafanaDashboards.distributedTracingOperations.enabled }}
{{- $environment := required "environment must be set through values-dev.yaml or values-prod.yaml" .Values.environment }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: distributed-tracing-operations-{{ $environment }}
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
    app.kubernetes.io/part-of: observability
    app.kubernetes.io/component: grafana-dashboard
    environment: {{ $environment | quote }}
  annotations:
    grafana_folder: {{ .Values.grafanaDashboards.distributedTracingOperations.folder | quote }}
data:
  distributed-tracing-operations-{{ $environment }}.json: |
{{ .Files.Get "dashboards/distributed-tracing-operations.json"
   | replace "__ENVIRONMENT__" $environment
   | replace "__NAMESPACE__" .Values.grafanaDashboards.distributedTracingOperations.namespace
   | indent 4 }}
{{- end }}
