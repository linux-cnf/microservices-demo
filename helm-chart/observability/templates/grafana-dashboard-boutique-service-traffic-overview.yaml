{{- if and .Values.grafanaDashboards.enabled .Values.grafanaDashboards.boutiqueServiceTrafficOverview.enabled }}
{{- $environment := required "environment must be set through values-dev.yaml or values-prod.yaml" .Values.environment }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: boutique-service-traffic-overview-{{ $environment }}
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
    app.kubernetes.io/part-of: observability
    app.kubernetes.io/component: grafana-dashboard
    environment: {{ $environment | quote }}
  annotations:
    grafana_folder: {{ .Values.grafanaDashboards.boutiqueServiceTrafficOverview.folder | quote }}
data:
  boutique-service-traffic-overview-{{ $environment }}.json: |
{{ .Files.Get "dashboards/boutique-service-traffic-overview.json"
   | replace "${environment}" $environment
   | indent 4 }}
{{- end }}
