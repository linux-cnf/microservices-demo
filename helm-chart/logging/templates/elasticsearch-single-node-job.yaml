{{- if .Values.singleNodeSettingsJob.enabled }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: elasticsearch-single-node-settings
  namespace: logging
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: elasticsearch-single-node-settings
  namespace: logging
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["elasticsearch-es-elastic-user"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: elasticsearch-single-node-settings
  namespace: logging
subjects:
  - kind: ServiceAccount
    name: elasticsearch-single-node-settings
    namespace: logging
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: elasticsearch-single-node-settings
---
apiVersion: batch/v1
kind: Job
metadata:
  name: elasticsearch-single-node-settings
  namespace: logging
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  template:
    spec:
      serviceAccountName: elasticsearch-single-node-settings
      restartPolicy: OnFailure
      containers:
        - name: set-es-replicas
          image: bitnami/kubectl:1.30
          command:
            - sh
            - -c
            - |
              ELASTIC_PASSWORD=$(kubectl get secret elasticsearch-es-elastic-user -n logging -o go-template='{{ "{{" }}.data.elastic | base64decode{{ "}}" }}')

              until curl -k -u elastic:${ELASTIC_PASSWORD} https://elasticsearch-es-http.logging.svc:9200/_cluster/health; do
                echo "Waiting for Elasticsearch..."
                sleep 10
              done

              curl -k -u elastic:${ELASTIC_PASSWORD} \
                -X PUT "https://elasticsearch-es-http.logging.svc:9200/_all/_settings?expand_wildcards=all" \
                -H "Content-Type: application/json" \
                -d '{"index":{"number_of_replicas":0}}'

              curl -k -u elastic:${ELASTIC_PASSWORD} \
                -X PUT "https://elasticsearch-es-http.logging.svc:9200/_index_template/single-node-defaults" \
                -H "Content-Type: application/json" \
                -d '{
                  "index_patterns": ["*"],
                  "template": {
                    "settings": {
                      "number_of_replicas": 0
                    }
                  },
                  "priority": 500
                }'
{{- end }}
