namespace: logging

agent:
  name: logging-agent
  version: "9.2.0"

elasticsearchRef:
  name: elasticsearch

tolerations:
  - operator: Exists

resources:
  requests:
    cpu: 20m
    memory: 100Mi
  limits:
    cpu: 100m
    memory: 200Mi
