namespace: logging

agent:
  name: logging-agent
  version: "9.2.0"

elasticsearchRef:
  name: elasticsearch

tolerations:
  - operator: Exists

resources:
  requests:
    cpu: 50m
    memory: 150Mi
  limits:
    cpu: 200m
    memory: 300Mi
