# Enforces namespace-level mTLS in STRICT mode for boutique.
# Only Istio sidecar-to-sidecar encrypted traffic is allowed.
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: boutique-mtls
spec:
  mtls:
    mode: STRICT
