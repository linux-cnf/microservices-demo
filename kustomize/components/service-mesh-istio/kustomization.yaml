# Enables Istio traffic-management resources for the boutique application. 
# This is the entry point that tells Kustomize which Istio manifests to include
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - allow-egress-googleapis.yaml
  - frontend.yaml
  - mtls.yaml
