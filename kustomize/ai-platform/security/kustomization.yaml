# ---------------------------------------------------------
# PURPOSE:
# Aggregates all AI security resources managed by Kustomize.
# Includes mTLS, authorization, routing, and traffic policies.
# ---------------------------------------------------------

resources:
  - peerauthentication.yaml
  - authorizationpolicy.yaml
  - destinationrule.yaml
  - virtualservice.yaml
  - authorizationpolicy-ai-bootstrap-to-ollama.yaml
  - networkpolicy.yaml
  - authorizationpolicy-ai-redis.yaml
