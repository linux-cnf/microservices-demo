# -------------------------------------------------------------------
# PURPOSE:
# Deploy LLM Gateway as the controlled API layer for AI requests.
#
# RESPONSIBILITIES:
# - Receive /chat requests
# - Forward prompts to Ollama runtime
# - Hide runtime implementation from frontend and future agents
#
# DESIGN:
# Frontend/Agent -> LLM Gateway -> Ollama today
# Frontend/Agent -> LLM Gateway -> vLLM later
#
# In short:
# Stable abstraction layer for all LLM traffic.
# -------------------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-llm-gateway
  namespace: ai
  labels:
    app: ai-llm-gateway
    workload: ai
    tier: platform
    purpose: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-llm-gateway
  template:
    metadata:
      labels:
        app: ai-llm-gateway
        workload: ai
        tier: platform
        purpose: gateway
    spec:
      nodeSelector:
        workload: ai
      tolerations:
        - key: workload
          operator: Equal
          value: ai
          effect: NoSchedule
      containers:
        - name: ai-llm-gateway
          image: us-central1-docker.pkg.dev/project-19d98bfe-795f-49b8-af0/ai-microservices/ai-llm-gateway:ef016d94f33580617f349d8bc5c65e42c1deceab
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: OLLAMA_URL
              value: "http://ai-ollama.ai.svc.cluster.local:11434"
            - name: DEFAULT_MODEL
              value: "qwen2.5:3b"
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
